import 'package:flutter/material.dart';

class ProfileShopChat extends StatelessWidget {
  const ProfileShopChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}